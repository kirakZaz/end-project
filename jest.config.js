const config = {
  verbose: true,
  globals: {
    document: {},
  },
};

module.exports = config;

module.exports = async () => {
  return {
    verbose: true,
    globals: {
      document: {},
    },
  };
};
